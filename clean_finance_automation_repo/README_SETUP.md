Quick setup:
1. Copy .env.example to .env.local and fill values.
2. npm install
3. npm run dev
4. Visit http://localhost:3000
5. To run the local scheduler: npm run scheduler (ensure MEGAPROMPT_SERVER_URL env var points to your /api/megaPromptServer endpoint)
